# SELA 完整修復包 (Final)

## 📦 完整檔案結構

```
app/
├── services/
│   └── auth.py              ← 認證服務（缺失的核心檔案）
├── routers/
│   ├── home.py              ← 首頁路由（修復版）
│   └── admin.py             ← 管理後台（修復版）
└── templates/partials/
    ├── home_groups.html     ← 首頁團單列表
    └── group_card.html      ← 團單卡片
```

## 🚀 部署步驟

```powershell
cd C:\Users\cbrto\Documents\Python\線上訂餐

# 1. 解壓 sela-final.zip 到專案目錄
#    選擇「全部覆蓋」

# 2. 確認檔案存在
dir app\services\auth.py

# 3. 部署
git add .
git commit -m "Restore auth.py and fix bugs"
git push --force
```

## ✅ 修復內容

1. **auth.py 缺失** - 重建認證服務
2. **時間到的團還在飲料區** - 加入 deadline 條件
3. **使用者頁面黑屏** - 註冊 taipei filter
4. **店家改分類黑屏** - 大小寫轉換修復
